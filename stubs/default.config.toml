# name | modified | created | size | date-in-path
sort_key = "name"

# asc | desc
sort_direction = "asc"

# grid | list | grouped
view = "grid"

# gallery | bookmarks | duplicates
page = "gallery"

# cover | contain
thumbnail_fit = "cover"

[presets.1]
sort_key = "date-in-path"
sort_direction = "desc"
thumbnail_fit = "contain"
