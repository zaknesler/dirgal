# name | modified | created | size | date-in-path
sort_key = "name"

# asc | desc
sort_direction = "asc"

# grid | list | grouped
view = "grid"

# gallery | bookmarks | duplicates
page = "gallery"

# fill | contain | cover | scale-down | none
thumbnail_fit = "cover"
